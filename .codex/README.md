# Snow AI Studio Codex 文档入口

本目录存放按需规则；常驻规则在上级 `AGENTS.md`，产品/架构/部署行为以 README 和
`docs/architecture.md` 为准。

- `workflows.md`：故障排查、逻辑审查、迁移/部署验证和 Git 流程。
- `docs/code-review.md`：Flask、数据库、Worker、渠道、文件和前端的 CR 清单。
- `docs/doc-writing.md`：文档分层、记录边界和命名。
- `skills/`：仅用户明确要求时运行的全量 Bug/性能扫描。

先读 `AGENTS.md`，再按任务读取目标 README 和相关文档；不要无边界扫描，规则只保留一个权威来源。
